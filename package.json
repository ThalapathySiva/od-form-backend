{
  "name": "od-form-back-end",
  "version": "1.0.0",
  "description": "Backend for OD-Form Registration",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch 'src /**' --ext 'ts,json' --ignore 'src/**/*.spec.ts' --exec 'ts-node src/index.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  }
}
